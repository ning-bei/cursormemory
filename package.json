{
  "name": "openmemory",
  "version": "0.1.0",
  "description": "CLI tool for building a persistent memory system across projects and documents",
  "type": "module",
  "bin": {
    "openmemory": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "postinstall": "npm run build",
    "link": "npm run build && npm link"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "chalk": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
